name: Apply M4 Slice 4

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'agent/m4-slice-4-max-iterations'
    runs-on: ubuntu-latest
    steps:
      - name: Check out the Slice 4 branch
        uses: actions/checkout@v4
        with:
          ref: agent/m4-slice-4-max-iterations

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Apply Slice 4
        run: |
          python3 scripts/apply-m4-slice4.py
          python3 scripts/fix-m4-slice4.py

      - name: Install locked dependencies
        run: npm ci

      - name: Run repository checks
        shell: bash
        run: |
          set +e
          npm run check > check.log 2>&1
          status=$?
          cat check.log
          exit $status

      - name: Upload check diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m4-slice4-check-log
          path: check.log

      - name: Commit Slice 4
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md docs skills src tests
          if git diff --cached --quiet; then
            echo "Slice 4 product files are already current."
          else
            git commit -m "Implement M4 Slice 4 hard iteration limits"
            git push origin HEAD:agent/m4-slice-4-max-iterations
          fi

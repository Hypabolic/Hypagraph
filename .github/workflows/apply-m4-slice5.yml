name: Apply M4 Slice 5

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'agent/m4-slice-5-progress-patience'
    runs-on: ubuntu-latest
    steps:
      - name: Check out the Slice 5 branch
        uses: actions/checkout@v4
        with:
          ref: agent/m4-slice-5-progress-patience

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Apply Slice 5
        shell: bash
        run: |
          python3 scripts/fix-m4-slice5.py
          python3 scripts/apply-m4-slice5.py

      - name: Install locked dependencies
        run: npm ci

      - name: Run repository checks
        shell: bash
        run: |
          set +e
          npm run check > check.log 2>&1
          status=$?
          cat check.log
          exit $status

      - name: Upload check diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m4-slice5-check-log
          path: check.log

      - name: Commit Slice 5
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md docs skills src tests
          if git diff --cached --quiet; then
            echo "Slice 5 product files are already current."
          else
            git commit -m "Implement M4 Slice 5 progress and patience"
            git push origin HEAD:agent/m4-slice-5-progress-patience
          fi

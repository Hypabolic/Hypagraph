import "./model.js";

declare module "./model.js" {
  interface FileAssertionCheckDefinition {
    /** Transitional source compatibility for Pi invocation labels. */
    command?: undefined;
    arguments?: undefined;
  }

  interface GitAssertionCheckDefinition {
    /** Transitional source compatibility for Pi invocation labels. */
    command?: undefined;
    arguments?: undefined;
  }
}
